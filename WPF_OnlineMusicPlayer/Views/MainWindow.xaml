<Window x:Class="WPF_OnlineMusicPlayer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WPF_OnlineMusicPlayer.ViewModels"
        Title="WPF Online Music Player (MVVM &amp; Database)" Height="500" Width="750" 
        Background="#121212" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <MediaElement x:Name="AudioPlayer" LoadedBehavior="Manual" UnloadedBehavior="Stop" Volume="0.8" 
                      MediaOpened="AudioPlayer_MediaOpened" MediaEnded="AudioPlayer_MediaEnded"/>

        <Border Grid.Column="0" Background="#181818" CornerRadius="10" Margin="0,0,15,0" Padding="20">
            <StackPanel VerticalAlignment="Center">

                <Border Width="180" Height="180" CornerRadius="10" Margin="0,0,0,20" Background="#282828">
                    <Image Source="{Binding CurrentTrack.image}" Stretch="UniformToFill"/>
                </Border>

                <TextBlock Text="{Binding CurrentTrack.name, FallbackValue='Chưa chọn bài hát'}" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding CurrentTrack.artist_name, FallbackValue='Nghệ sĩ'}" Foreground="#B3B3B3" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="lblCurrentTime" Text="00:00" Foreground="#B3B3B3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Slider x:Name="slTime" Grid.Column="1" Minimum="0" Maximum="100" Value="0" 
                            PreviewMouseLeftButtonDown="slTime_DragStart" 
                            PreviewMouseLeftButtonUp="slTime_DragEnd" VerticalAlignment="Center"/>
                    <TextBlock x:Name="lblTotalTime" Text="00:00" Foreground="#B3B3B3" Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="⏪" Width="40" Height="40" Margin="5" Background="#282828" Foreground="#B3B3B3" FontSize="16" BorderThickness="0" Click="BtnRewind_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="⏮" Width="40" Height="40" Margin="5" Background="#282828" Foreground="White" FontSize="16" BorderThickness="0" Click="BtnPrev_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="btnPlayPause" Content="▶" Width="55" Height="55" Margin="10,0" Background="#1DB954" Foreground="White" FontSize="24" FontWeight="Bold" BorderThickness="0" Click="BtnPlayPause_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="27.5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="⏭" Width="40" Height="40" Margin="5" Background="#282828" Foreground="White" FontSize="16" BorderThickness="0" Click="BtnNext_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="⏩" Width="40" Height="40" Margin="5" Background="#282828" Foreground="#B3B3B3" FontSize="16" BorderThickness="0" Click="BtnForward_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#181818" CornerRadius="10" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" Margin="0,0,10,0" Background="#282828" Foreground="White" BorderThickness="0"/>
                    <Button Grid.Column="1" Command="{Binding SearchCommand}" Content="Tìm kiếm" Width="80" Height="30" Background="#282828" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <TextBlock Grid.Row="1" Text="Trending Music (Online)" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Command="{Binding LoadMusicCommand}" Content="Khám phá" Width="100" Height="30" Margin="0,0,10,0" Background="#1DB954" Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Command="{Binding RecommendCommand}" Content="✨ Gợi ý cho tôi" Width="120" Height="30" Background="#8A2BE2" Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <ListBox x:Name="Playlist" Grid.Row="3" Background="Transparent" BorderThickness="0" 
                         ItemsSource="{Binding Playlist}" SelectedItem="{Binding SelectedTrack, Mode=TwoWay}"
                         SelectionChanged="Playlist_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <Image Source="{Binding image}" Width="40" Height="40" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding name}" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding artist_name}" Foreground="#B3B3B3" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>